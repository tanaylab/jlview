# Check if an object is a jlview ALTREP vector

Check if an object is a jlview ALTREP vector

## Usage

``` r
is_jlview(x)
```

## Arguments

- x:

  An R object

## Value

`TRUE` if `x` is a jlview ALTREP vector, `FALSE` otherwise
